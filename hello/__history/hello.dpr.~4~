program hello;

uses
  System.StartUpCopy,
  FMX.Forms,
  hello1 in 'hello1.pas' {Form1},
  hello2 in 'hello2.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
